import { Resolvers as ResolversType, IResolvers } from '../generated/resolvers.types'
import { Query, Mutation} from './resolvers';

const Resolvers: ResolversType = {
  Query, Mutation
};

export default Resolvers;
